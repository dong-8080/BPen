<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
      <!-- CardView 的背景颜色 -->
     <!-- 填充的颜色 -->
    <!-- 边框宽度 -->
    <stroke
        android:width="2dp"
        android:color="@color/doderBlue" /> <!-- 边框颜色 -->
    <corners android:radius="30dp" /> <!-- CardView 的圆角半径 -->
</shape>
